import scala.math

object math {
  def main(args : Array[String]){
    //rounding
    var x : Double = 2.5
    println(Math.round(x))

    //max min
    println(Math.max(1, 2))
    println(Math.min(1, 2))

    //random
    println (Math.random())

  }
}
